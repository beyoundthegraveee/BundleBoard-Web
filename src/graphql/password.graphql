mutation RequestPasswordChange($input: PasswordChangeInput!) {
  requestPasswordChange(input: $input) {
    success
    message
    attemptsLeft
  }
}

mutation ConfirmPasswordChange($code: String!) {
  confirmPasswordChange(code: $code) {
    success
    message
    attemptsLeft
  }
}